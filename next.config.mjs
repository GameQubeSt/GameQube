/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Lint is optional for this demo; don't fail production builds on lint.
  eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;
